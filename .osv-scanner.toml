# OSV-Scanner Configuration
# https://google.github.io/osv-scanner/configuration/

# Enable required plugins
[PluginConfig]
# Enable the java/archive plugin to prevent warnings
[[PluginConfig.Enable]]
name = "java/archive"

# Configure scanning behavior
[ScanConfig]
# Skip git history scanning to avoid false positives
SkipGit = true
